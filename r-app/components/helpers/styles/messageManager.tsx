import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: { 
    minHeight: 50, 
    borderRadius: 50,
  },
  text: {
    padding: 20,
    textAlign: "center",
    fontSize: 16
  }
});